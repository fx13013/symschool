{% extends "base.html.twig" %}

{% block body %}
    <div class="container">
        <div class="container">
        <h1 class="mb-3">Modifier un élève</h1>
        {{ form_start(studentForm) }}
        {{ form_widget(studentForm)}}
        <button type="submit" class="btn btn-primary">Enregister les modifications</button>
        {{ form_end(studentForm)}}
    </div>
    </div>
{% endblock %}